# E:\MCU\esp32\Myesps3Project\wifi_connect 【角色与架构目标】 你是一个资深的 ESP-IDF / FreeRTOS 嵌入式架构师。我希望彻底净化 main.c，使其成为一个纯粹的“启动台 (Launcher)”。 要求遵循“高内聚与模块自治”的原则：各个业务模块自己负责创建和管理自己的 FreeRTOS Task，不要把任务实体暴露在 main.c 中，也不需要新建多余的文件。 【执行指令】 第一步：改造 app_doubao_client.c 及其头文件 (承接大脑任务) 在 app_doubao_client.c 顶部增加必要的头文件引入：#include "app_message.h" 和 #include "app_video_server.h"。 将当前 main.c 中完整的 static void AI_Brain_Task(void *pvParameters) 函数剪切到 app_doubao_client.c 中，并重命名为 static void doubao_message_task(void *pvParameters)。 注意：必须完全保留该任务内部针对 xQueue_Brain 的 switch-case 逻辑（收到文本调 App_Doubao_Post，收到图像指令调 App_Video_Capture_And_Recognize）。 删除 app_doubao_client.c 里之前写的老的测试任务 void doubao_task(void *pvParameters)。 修改 app_doubao_client.c 中的 void App_Doubao_Task_Start(void) 函数，使其内部调用：xTaskCreate(doubao_message_task, "doubao_task", 16384, NULL, 5, NULL);（注意栈空间为 16KB）。 确保 app_doubao_client.h 中已经对外声明了 void App_Doubao_Task_Start(void);。 第二步：极致净化 main.c 从 main.c 中彻底删除原有的 AI_Brain_Task 函数体。 修改 app_main 函数，使其仅包含系统和各大模块的初始化及启动调用。重写为以下极简结构： C void app_main(void) { // 1. 基础环境与总线初始化 App_WiFi_System_Init(); App_Message_Init(); // 2. 启动各大独立模块的后台任务 Video_Server_System_Init(); // 启动图传推流服务器 App_UART_Task_Start(); // 启动串口/控制台监听任务 App_Doubao_Task_Start(); // 启动大模型认知任务 // 3. 主循环空闲，让出 CPU while (1) { vTaskDelay(pdMS_TO_TICKS(1000)); } } 第三步：安全边界要求 请严格只做上述代码的物理位置搬运和重组。绝对不要修改底层的 App_Doubao_Post HTTP 请求逻辑，也绝对不要修改 app_uart.c、app_message.c 和 app_video_server.c 中的任何代码。 请直接输出修改后的 app_doubao_client.c 和 main.c 的完整代码。

- Session ID: 019cc21b-1608-7602-bf47-b8d7bce9df17
- Title: E:\MCU\esp32\Myesps3Project\wifi_connect 【角色与架构目标】 你是一个资深的 ESP-IDF / FreeRTOS 嵌入式架构师。我希望彻底净化 main.c，使其成为一个纯粹的“启动台 (Launcher)”。 要求遵循“高内聚与模块自治”的原则：各个业务模块自己负责创建和管理自己的 FreeRTOS Task，不要把任务实体暴露在 main.c 中，也不需要新建多余的文件。 【执行指令】 第一步：改造 app_doubao_client.c 及其头文件 (承接大脑任务) 在 app_doubao_client.c 顶部增加必要的头文件引入：#include "app_message.h" 和 #include "app_video_server.h"。 将当前 main.c 中完整的 static void AI_Brain_Task(void *pvParameters) 函数剪切到 app_doubao_client.c 中，并重命名为 static void doubao_message_task(void *pvParameters)。 注意：必须完全保留该任务内部针对 xQueue_Brain 的 switch-case 逻辑（收到文本调 App_Doubao_Post，收到图像指令调 App_Video_Capture_And_Recognize）。 删除 app_doubao_client.c 里之前写的老的测试任务 void doubao_task(void *pvParameters)。 修改 app_doubao_client.c 中的 void App_Doubao_Task_Start(void) 函数，使其内部调用：xTaskCreate(doubao_message_task, "doubao_task", 16384, NULL, 5, NULL);（注意栈空间为 16KB）。 确保 app_doubao_client.h 中已经对外声明了 void App_Doubao_Task_Start(void);。 第二步：极致净化 main.c 从 main.c 中彻底删除原有的 AI_Brain_Task 函数体。 修改 app_main 函数，使其仅包含系统和各大模块的初始化及启动调用。重写为以下极简结构： C void app_main(void) { // 1. 基础环境与总线初始化 App_WiFi_System_Init(); App_Message_Init(); // 2. 启动各大独立模块的后台任务 Video_Server_System_Init(); // 启动图传推流服务器 App_UART_Task_Start(); // 启动串口/控制台监听任务 App_Doubao_Task_Start(); // 启动大模型认知任务 // 3. 主循环空闲，让出 CPU while (1) { vTaskDelay(pdMS_TO_TICKS(1000)); } } 第三步：安全边界要求 请严格只做上述代码的物理位置搬运和重组。绝对不要修改底层的 App_Doubao_Post HTTP 请求逻辑，也绝对不要修改 app_uart.c、app_message.c 和 app_video_server.c 中的任何代码。 请直接输出修改后的 app_doubao_client.c 和 main.c 的完整代码。
- Message Count: 1
- User Turns: 1
- Original CWD: \\?\E:\codex
- Original Rollout: C:\Users\30817\.codex\sessions\2026\03\06\rollout-2026-03-06T15-44-40-019cc21b-1608-7602-bf47-b8d7bce9df17.jsonl
- Exported At: 2026-05-20T10:56:21.580Z

---

## 1. User
- Timestamp: 2026年3月6日 15:47:49

E:\MCU\esp32\Myesps3Project\wifi_connect 【角色与架构目标】 你是一个资深的 ESP-IDF / FreeRTOS 嵌入式架构师。我希望彻底净化 main.c，使其成为一个纯粹的“启动台 (Launcher)”。 要求遵循“高内聚与模块自治”的原则：各个业务模块自己负责创建和管理自己的 FreeRTOS Task，不要把任务实体暴露在 main.c 中，也不需要新建多余的文件。
【执行指令】
第一步：改造 app_doubao_client.c 及其头文件 (承接大脑任务)
在 app_doubao_client.c 顶部增加必要的头文件引入：#include "app_message.h" 和 #include "app_video_server.h"。
将当前 main.c 中完整的 static void AI_Brain_Task(void *pvParameters) 函数剪切到 app_doubao_client.c 中，并重命名为 static void doubao_message_task(void *pvParameters)。
注意：必须完全保留该任务内部针对 xQueue_Brain 的 switch-case 逻辑（收到文本调 App_Doubao_Post，收到图像指令调 App_Video_Capture_And_Recognize）。
删除 app_doubao_client.c 里之前写的老的测试任务 void doubao_task(void *pvParameters)。
修改 app_doubao_client.c 中的 void App_Doubao_Task_Start(void) 函数，使其内部调用：xTaskCreate(doubao_message_task, "doubao_task", 16384, NULL, 5, NULL);（注意栈空间为 16KB）。
确保 app_doubao_client.h 中已经对外声明了 void App_Doubao_Task_Start(void);。
第二步：极致净化 main.c
从 main.c 中彻底删除原有的 AI_Brain_Task 函数体。
修改 app_main 函数，使其仅包含系统和各大模块的初始化及启动调用。重写为以下极简结构：
C
void app_main(void)
{   
    // 1. 基础环境与总线初始化
    App_WiFi_System_Init();
    App_Message_Init();

    // 2. 启动各大独立模块的后台任务
    Video_Server_System_Init(); // 启动图传推流服务器
    App_UART_Task_Start();      // 启动串口/控制台监听任务
    App_Doubao_Task_Start();    // 启动大模型认知任务
    
    // 3. 主循环空闲，让出 CPU
    while (1) {
        vTaskDelay(pdMS_TO_TICKS(1000));
    }
}

第三步：安全边界要求 请严格只做上述代码的物理位置搬运和重组。绝对不要修改底层的 App_Doubao_Post HTTP 请求逻辑，也绝对不要修改 app_uart.c、app_message.c 和 app_video_server.c 中的任何代码。 请直接输出修改后的 app_doubao_client.c 和 main.c 的完整代码。

<turn_aborted>
The user interrupted the previous turn on purpose. Any running unified exec processes were terminated. If any tools/commands were aborted, they may have partially executed; verify current state before retrying.
</turn_aborted>
